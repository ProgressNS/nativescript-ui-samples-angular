# RadCartesianChart and RadPieChart NativeScript Angular demo application

This application showcases scenarios of using the `RadCartesianChart` and `RadPieChart` inside a NativeScript + Angular application.